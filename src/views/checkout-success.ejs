<%- include('partials/header') %>

<div class="not-found">
  <h1>Спасибо за заказ!</h1>
  <p>Ссылка на страницу заказа ушла на вашу почту. Все свои заказы, файлы и экземпляры можно смотреть в <a href="/my">кабинете</a> — вход по почте, без пароля.</p>

  <% if (typeof downloads !== 'undefined' && downloads.length > 0) { %>
    <h2>Ваши файлы</h2>
    <p>Ссылки продублированы письмом на <%= order.email %>. Сохраните файлы на устройство.</p>
    <div class="download-list">
      <% downloads.forEach(function(item) { %>
        <div class="download-item">
          <div class="download-item__name"><%= item.product_name %></div>
          <a class="btn" href="/downloads/<%= item.token %>">Скачать</a>
        </div>
      <% }); %>
    </div>
  <% } %>

  <a class="btn" href="/catalog">Вернуться в каталог</a>
  <p class="hint">Пока собираем заказ, можно <a href="/games">поиграть</a>: пять секунд трека — и попробуйте угадать, какой.</p>
</div>

<%- include('partials/footer') %>
