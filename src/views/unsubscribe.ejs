<%- include('partials/header') %>

<% if (ok) { %>
  <h1>Вы отписаны</h1>
  <p>Адрес <strong><%= email %></strong> удалён из рассылки новостей. Если передумаете — форма подписки всегда на сайте.</p>
<% } else { %>
  <h1>Ссылка не сработала</h1>
  <p>Похоже, ссылка отписки повреждена или устарела. Напишите нам на info@levkeiser.com — отпишем вручную.</p>
<% } %>

<%- include('partials/footer') %>
