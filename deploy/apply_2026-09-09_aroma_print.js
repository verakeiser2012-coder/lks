// Карта ароматов перестаёт быть файлом и становится печатным плакатом.
// Ищем по слагу: id на сервере и локально разные.

const db = require('../src/db');

const SLUG = 'karta-naturalnyh-aromatov';

const DESC =
  'Плакат: карта натуральных ароматов. 166 материалов парфюмерии и благовоний на двенадцати ' +
  'семействах. Уд, лабданум, бахур, морёный дуб, ладан, сандал, амбра — рядом с цитрусами, ' +
  'травами, пряностями и цветами.\n\n' +
  'Наша разработка. Прямого аналога мы не нашли. Есть колесо ароматов Эдвардса — но это круг из ' +
  'одних семейств, без самих материалов. Есть карты готовых духов — но не сырья. Есть научный граф ' +
  'вкусов — но про еду. Здесь другое:\n\n' +
  '— на одной карте стоят натуральные материалы парфюмерии и благовоний, а не абстрактные ' +
  'категории и не готовые ароматы;\n' +
  '— место точки вычисляется из состава, а не расставляется на глаз: чем сильнее семейство звучит ' +
  'в аромате, тем ближе он к нему, поэтому положение на карте — это уже формула;\n' +
  '— состав виден прямо в точке круговой диаграммой: сразу считывается, из чего аромат собран.\n\n' +
  'Скажем честно, как есть: мы искали, но весь интернет не перелопатили. Утверждать «нигде такого ' +
  'нет» не будем — а вот «прямого аналога не нашли» скажем твёрдо.\n\n' +
  'Печать\n' +
  'Формат A1, 841 × 594 мм. Матовая бумага 250 г/м², пигментная печать, 254 точки на дюйм — ' +
  'подписи мелких материалов читаются с расстояния вытянутой руки. Печатаем в Екатеринбурге под ' +
  'заказ, поэтому на изготовление нужно несколько дней. Отправляем в тубусе.\n\n' +
  'Собрано по колесу ароматов М. Эдвардса, категориям Foodpairing и Flavor Network, дескрипторам ' +
  'The Good Scents и классификации натурального сырья.\n\n' +
  'Интерактивная версия карты открыта бесплатно: [levkeiser.com/aroma](/aroma) — там можно навести ' +
  'на любой материал и посмотреть его состав, а ещё собрать свою смесь и увидеть, где она встанет.';

const INCLUDES = [
  'Плакат A1, 841 × 594 мм',
  'Матовая бумага 250 г/м²',
  'Пигментная печать 254 dpi',
  'Отправка в тубусе',
].join('\n');

const p = db.prepare('SELECT id, is_digital FROM products WHERE slug = ?').get(SLUG);
if (!p) throw new Error('товар не найден: ' + SLUG);

db.prepare(
  `UPDATE products SET description = ?, price = ?, includes = ?, stock = ?,
   is_digital = 0, digital_file = '', digital_filename = '', digital_size = 0,
   dimensions = ?, material = ?, lead_time = ?
   WHERE slug = ?`
).run(DESC, 2400.0, INCLUDES, 999, '841 × 594 мм (A1)', 'Матовая бумага 250 г/м²',
      'Печатаем под заказ, 3–5 дней', SLUG);

const r = db.prepare(
  `SELECT p.id, p.name, p.price, p.is_digital, p.stock, c.name AS cat
   FROM products p JOIN categories c ON c.id = p.category_id WHERE p.slug = ?`
).get(SLUG);
console.log('было цифровым:', p.is_digital ? 'да' : 'нет');
console.log('стало:', r.id, r.name, '|', r.cat, '|', r.price, '₽ | цифровой:', r.is_digital ? 'да' : 'нет');
